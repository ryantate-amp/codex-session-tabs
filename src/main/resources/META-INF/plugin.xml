<idea-plugin>
    <id>com.github.ryantateamp.codexsessiontabs</id>
    <name>Codex Session Tabs</name>
    <vendor url="https://github.com/ryantate-amp">Ryan Tate</vendor>

    <description><![CDATA[
        <p>
            Keep Codex terminal sessions easy to identify and ready to resume inside JetBrains IDEs.
        </p>
        <ul>
            <li><strong>Session-aware tab names:</strong> Keep each terminal tab synchronized with its
                Codex session name.</li>
            <li><strong>Native activity indicator:</strong> Preserve JetBrains' Codex icon and show its
                animated progress state while Codex is working.</li>
            <li><strong>Session restoration:</strong> Restore the Codex sessions that were open when the
                IDE closed, including reusable CLI flags such as <code>--yolo</code>.</li>
        </ul>
        <p>
            Codex Session Tabs works automatically with the Reworked Terminal. It uses local process
            and Codex state information, has no external service, and does not copy transcript contents.
        </p>
    ]]></description>

    <change-notes><![CDATA[
        <ul>
            <li>Restore only Codex sessions whose terminal tabs were open at shutdown.</li>
            <li>Preserve the visible terminal tab order across IDE restarts.</li>
            <li>Retain reusable launch flags such as <code>--yolo</code> without duplicating
                <code>resume</code> arguments.</li>
            <li>Recognize sessions when Codex app-server remote control owns the rollout process.</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.plugins.terminal</depends>

    <extensions defaultExtensionNs="com.intellij">
        <projectService serviceImplementation="com.github.ryantateamp.codexsessiontabs.CodexSessionTabsService"/>
        <postStartupActivity implementation="com.github.ryantateamp.codexsessiontabs.CodexSessionTabsStartupActivity"/>
    </extensions>
</idea-plugin>
